class AppConstants {
  static const kPadding = 16.0;
  static const kBorderRadius = 8.0;
  static const kAnimationDuration = Duration(milliseconds: 300);

  static const storageBasePath = 'community_posts';
  static const maxFileSize = 10 * 1024 * 1024; // 10MB in bytes
  static const snackBarDuration = Duration(seconds: 3);

  static const maxVideoLength = Duration(minutes: 5);
  static const maxImageDimension = 1920;
  static const imageQuality = 80;
}